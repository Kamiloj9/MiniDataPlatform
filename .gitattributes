# Enforce LF line endings for shell scripts and config files
*.sh text eol=lf
*.py text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.env text eol=lf
*.conf text eol=lf
Dockerfile text eol=lf

# Treat all text files as UTF-8
* text=auto

# Optional: Exclude Jupyter notebook metadata diffs (if you use notebooks)
*.ipynb -diff
